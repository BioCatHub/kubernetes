apiVersion: v1
kind: Service
metadata:
  name: {{ include "acme-cluster-issuer.fullname" . }}
  labels:
    {{- include "acme-cluster-issuer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "acme-cluster-issuer.selectorLabels" . | nindent 4 }}
