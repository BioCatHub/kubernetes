apiVersion: v1
kind: Service
metadata:
  name: {{ include "biocathub.fullname" . }}
  labels:
    {{- include "biocathub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "biocathub.selectorLabels" . | nindent 4 }}
