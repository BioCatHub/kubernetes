apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "biocathub.fullname" . }}-test-connection"
  labels:
    {{- include "biocathub.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "biocathub.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
